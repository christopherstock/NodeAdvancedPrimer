
    /**
    *   The state of the 'TaskInput' component.
    */
    export interface TaskInputState
    {
        /** Indicates if an input error is currently present. */
        inputError :boolean;

        /** The current content of the input field. */
        inputText  :string;
    }
